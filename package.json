{
  "name": "econ-fv-test-v1-6",
  "version": "1.6.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "check": "node scripts/check-functions.mjs",
    "verify": "node scripts/verify-repo.mjs",
    "build": "npm run check && npm run verify",
    "test": "npm run build"
  },
  "dependencies": {
    "@netlify/blobs": "10.7.9",
    "@netlify/functions": "5.3.0"
  },
  "devDependencies": {
    "netlify-cli": "26.1.0"
  }
}
